# Port for the proxy server
PORT=5001

# Your main backend URL (where the actual webhook endpoint is)
TARGET_BACKEND_URL=http://localhost:5000

# Webhook secret (must match your backend WEBHOOK_SECRET)
WEBHOOK_SECRET=73oTEr>n/4X


